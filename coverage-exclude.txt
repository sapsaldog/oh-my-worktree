# Coverage exclusions — files intentionally NOT unit-tested.
# One glob per line, relative to repo root. `dir/**` excludes a whole subtree.
# Every entry must state a reason. See
# docs/superpowers/specs/2026-06-03-coverage-100-gate-design.md.

# SwiftUI view bodies — not unit-tested (excluded by design).
OhMyWorktree/Views/**

# Menu-bar / app lifecycle glue — requires a running NSApplication.
OhMyWorktree/AppDelegate.swift
OhMyWorktree/AppDelegate+*.swift
OhMyWorktree/OhMyWorktreeApp.swift

# TEMPORARY — flaky / non-portable coverage. These files are exercised through
# concurrency races (async job-queue timeouts, cancellation) or system paths
# whose executed lines vary run-to-run AND differ between local and CI (Xcode
# 16.4, headless). A strict per-file floor would flake. Measured on CI across 3
# runs: BackgroundTaskQueue 266–284/304; Repository 7–10/10; WorktreeListViewModel
# 334–338/482. Others (Worktree, ExternalToolLauncher, …) read far lower on CI
# than locally (no external tools installed; async timing differs). Remove each
# entry once its tests are made deterministic (inject clock/scheduler/process
# seams) and the file reaches 100%. See the spec's "Nondeterministic coverage
# (flaky quarantine)" section.
#
# (a) Coverage varies run-to-run (nondeterministic on CI):
OhMyWorktree/Services/BackgroundTaskQueue.swift
OhMyWorktree/Services/HotkeyManager.swift
OhMyWorktree/Services/WorktreeManager.swift
OhMyWorktree/Services/WorktreeManager+GitOps.swift
OhMyWorktree/Models/OhMyWorktreeError.swift
OhMyWorktree/Models/Repository.swift
OhMyWorktree/Services/GitCommandExecutor.swift
OhMyWorktree/ViewModels/WorktreeListViewModel.swift
# Split out of WorktreeListViewModel to satisfy file/type length limits; carries the
# same async worktree-creation / detail-loading paths (filteredWorktrees IS tested via
# WorktreeListViewModelSearchTests, but the file is quarantined with its parent).
OhMyWorktree/ViewModels/WorktreeListViewModel+Creation.swift
# Same async-glue category: copied-files browser state + the apply-to-main
# coordinator (file write via CopiedFileDiffer + detail reload). Setters are
# trivially testable but quarantined with their parent VM.
OhMyWorktree/ViewModels/WorktreeListViewModel+CopiedFiles.swift
#
# (b) Coverage stable but much lower on CI than locally (not portable yet):
OhMyWorktree/Models/Worktree.swift
OhMyWorktree/Services/ExternalToolLauncher.swift
OhMyWorktree/ViewModels/WorktreeListViewModel+ExternalTools.swift

# OS-framework wrappers — a unit-test process cannot execute these lines:
#   GitHeadMonitor  — DispatchSource file watcher on .git/HEAD
#   WindowObserver  — AppKit NSWindow observation
#   NotificationManager — UNUserNotificationCenter (auth denied/headless on CI)
#   UpdaterManager  — Sparkle SPUStandardUpdaterController
# Their logic is mostly irreducible system glue; excluded by design. (Could be
# split into a thin seam + testable core later, but not worth it for now.)
OhMyWorktree/Services/GitHeadMonitor.swift
OhMyWorktree/Services/WindowObserver.swift
OhMyWorktree/Services/NotificationManager.swift
OhMyWorktree/Services/UpdaterManager.swift

# Logging seam — irreducible Logger/bundle-id autoclosures (see AppLog.swift).
OhMyWorktree/Services/AppLog.swift

# Thin system-call adapters (irreducible seams) are appended here as they are
# extracted in later phases, e.g.:
# OhMyWorktree/Services/<Name>+SystemSeam.swift
